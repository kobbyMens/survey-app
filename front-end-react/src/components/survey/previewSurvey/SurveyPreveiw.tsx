import { FC } from "react";
import styled from "styled-components";

// ===============Style Components====================================>
const SurveyPreviewContainer = styled.div`
  width: 100%;
  height: 100%;
`;

const SurveyPreview: FC = () => {
  return <SurveyPreviewContainer></SurveyPreviewContainer>;
};

export default SurveyPreview;
