import { FC } from "react";
import styled from "styled-components";

// ===============Style Components====================================>
const StyledSurveyThemesContainer = styled.div`
  width: 100%;
  height: 100%;
`;

const SurveyThemes: FC = () => {
  return <StyledSurveyThemesContainer></StyledSurveyThemesContainer>;
};

export default SurveyThemes;
